version: 2

sources:
  - name: arth_bq
    database: arth-insight-v2  # Your Project ID
    schema: stock_raw_data  # 👈 CHANGE THIS to the actual dataset name
    
    tables:
      - name: stock_intelligence_v3 # 👈 This is the table name
        config:
          loaded_at_field: last_updated
          freshness:
            warn_after: {count: 2, period: hour}
            error_after: {count: 72, period: hour}